јвторизаци€

1) –азверните репозиторий со своим проектом в директориию /home/box

2) –еализуйте view  дл€ регистрации новых пользователей

URL = /signup/

username - им€ пользовател€, логин
email - email пользовател€
password - пароль пользовател€

ѕри GET запросе должна отображатьс€ форма дл€ ввода данных, при POST запросе создаетс€ новый пользователей, осуществл€етс€ вход (login) созданного пользовател€ на сайт, возвращаетс€ редирект на главную страницу.

2) –еализуйте view дл€ входа на сайт (login) существующих пользователей.

URL = /login/

username - им€ пользовател€
password - пароль пользовател€


ѕри GET запросе должна отображатьс€ форма дл€ ввода данных, при POST запросе происходит вход (login) на сайт, возвращаетс€ редирект на главную страницу. 
»мена POST параметров важны!

3) »змените формы добавлени€ вопроса (AskForm) и ответа (AnswerForm) так что бы они учитывали текущего пользовател€ и сохран€ли его в поле author вопросов и ответов.  онструкторы форм должны получать стандартные дл€ Django-форм аргументы, т.е. должна быть возможность создать объект формы как AskForm() или AnswerForm(). “екущего пользовател€ можно добавл€ть в форму после создани€, например так:

form = AskForm(request.POST)
form._user = request.user


 4) Ќе забудьте закомитить и сохранить на github созданные файлы.
ѕодробнее про встроенную систему авторизации Django можно прочитать тут:

    https://docs.djangoproject.com/en/1.8/topics/auth/default/#creating-users
    https://docs.djangoproject.com/en/1.8/topics/auth/default/#django.contrib.auth.login
    https://docs.djangoproject.com/en/1.8/topics/auth/default/#authenticating-users